


cfg:

  logging:
    sources:
      std-out:
        logTolerance: "DEBUG"
        colorMode: "Extended"
  telemetry:
    sources:
      logged:
        logTolerance: "TRACE"

  logEntry:
    db:
      host: "${CFG.cfg.app.name}--db--${ENV.PETAFORM_ENV}"
      port: 5432
      exposedPort: 5111

